[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "security-tool-transitive-parent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests==2.33.0",
]

[tool.setuptools]
packages = ["transitive_parent"]
